# 故障排查指南

## 🔴 IDE 中看到红色错误？

如果您在 IDE（如 VSCode/Cursor）中看到红色错误标记，但代码实际上是正确的，请按照以下步骤排查：

## ✅ 解决方案

### 步骤 1: 安装/更新依赖

```bash
# 在项目根目录运行
flutter pub get
```

或

```bash
dart pub get
```

**这是最常见的原因**！依赖未安装时，IDE 无法解析导入。

### 步骤 2: 清理并重新获取依赖

```bash
# 清理
flutter clean

# 重新获取依赖
flutter pub get
```

### 步骤 3: 重启 IDE

有时 IDE 的缓存会导致错误显示：

1. 关闭 VSCode/Cursor
2. 重新打开项目
3. 等待 Dart Analysis Server 完成分析

### 步骤 4: 重启 Dart Analysis Server

在 VSCode/Cursor 中：
1. 按 `Ctrl+Shift+P`（或 `Cmd+Shift+P` on Mac）
2. 输入 "Dart: Restart Analysis Server"
3. 选择并执行

### 步骤 5: 检查 Flutter SDK

```bash
# 检查 Flutter 版本
flutter --version

# 检查 Dart 版本
dart --version

# 运行 Flutter doctor
flutter doctor
```

## 🔍 具体错误类型排查

### 错误类型 1: "Target of URI doesn't exist"

**原因**: 导入路径错误或依赖未安装

**解决方案**:
```bash
flutter pub get
```

### 错误类型 2: "The method 'xxx' isn't defined"

**原因**: 
- 类型未导入
- 依赖未安装
- IDE 缓存问题

**解决方案**:
1. 检查导入语句是否正确
2. 运行 `flutter pub get`
3. 重启 Dart Analysis Server

### 错误类型 3: "Undefined class 'XXX'"

**原因**: 
- 缺少导入
- 类名拼写错误
- 依赖问题

**解决方案**:
1. 检查是否有 `import` 语句
2. 确认类名拼写
3. 运行 `flutter pub get`

## 📋 已修复的问题

### ✅ 修复 #1: geeknow_service.dart 导入问题
- 添加了正确的 import 语句
- 修正了类名（VeoTaskStatus）
- 移除了错误的 export

### ✅ 修复 #2: examples 文件导入路径
- 修复了 3 个文件的导入路径
- 从 `../lib/...` 改为 `package:xinghe_new/...`

### ✅ 修复 #3: pubspec.yaml 依赖
- 添加了 `test: ^1.24.0` 依赖

## 🧪 验证步骤

### 1. 检查 Linter

```bash
# 使用 Flutter
flutter analyze

# 或使用 Dart
dart analyze
```

**预期输出**: `No issues found!`

### 2. 检查特定文件

```bash
dart analyze lib/services/api/providers/geeknow_service.dart
dart analyze lib/services/api/providers/openai_service.dart
dart analyze lib/services/api/providers/veo_video_service.dart
```

### 3. 运行测试

```bash
flutter test
```

## 🎯 当前项目状态

根据 linter 检查结果：

✅ **lib 目录**: 0 个错误  
✅ **examples 目录**: 0 个错误  
✅ **test 目录**: 0 个错误  
✅ **pubspec.yaml**: 配置正确  

**结论**: **代码本身没有错误**

## 💡 常见误解

### ❌ 红色波浪线 ≠ 一定有错误

IDE 中的红色波浪线可能是：
- 🔄 分析服务器正在分析
- 💾 缓存未更新
- 📦 依赖未安装
- 🔌 Analysis Server 未启动

### ✅ 真正的错误检查方式

运行命令行工具：
```bash
flutter analyze
# 或
dart analyze
```

只有命令行工具报告的才是真正的错误。

## 🚀 快速修复清单

按顺序尝试：

- [ ] 1. 运行 `flutter pub get`（**最重要**）
- [ ] 2. 重启 Dart Analysis Server
- [ ] 3. 清理项目 `flutter clean`
- [ ] 4. 重新安装依赖 `flutter pub get`
- [ ] 5. 重启 IDE
- [ ] 6. 检查 Flutter SDK 是否正常 `flutter doctor`

## 📞 仍然有问题？

如果按照上述步骤仍然有错误：

1. **截图错误信息**：具体的错误提示是什么？
2. **运行命令检查**：`flutter analyze` 的输出是什么？
3. **查看具体文件**：哪个文件显示红色？哪一行？
4. **检查终端输出**：是否有编译错误？

---

**当前状态**: ✅ **代码无错误**  
**Linter 结果**: ✅ **0 个问题**  
**建议操作**: 运行 `flutter pub get` 并重启 IDE
