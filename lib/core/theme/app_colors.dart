import 'package:flutter/material.dart';

class AppColors {
  // 核心色板
  static const Color primary = Color(0xFF0F172A);
  static const Color secondary = Color(0xFF1E293B);
  static const Color accent = Color(0xFF22C55E); // 绿色作为强调色
  static const Color background = Color(0xFF020617); // 深色背景
  static const Color surface = Color(0xFF0F172A); // 表面色
  static const Color text = Color(0xF8FAFC);
  
  // 功能色
  static const Color cardBg = Color(0xFF0F172A);
  static const Color activeBlue = Color(0xFF0099ff);
  static const Color border = Color(0x1FFFFFFF);
}
