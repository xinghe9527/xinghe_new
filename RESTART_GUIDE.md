# 应用重启指南

## 🔴 问题：按 F5 无法启动，dart.exe 进程残留

### 原因
- 之前的 Dart 进程没有正常退出
- 端口被占用
- 多个 dart.exe 实例冲突

### ✅ 解决方案

#### 方法 1：在 VSCode/Cursor 中操作（推荐）

1. **停止所有运行中的调试会话**
   - 点击 VSCode/Cursor 顶部的红色"停止"按钮
   - 或按 `Shift + F5` 停止调试

2. **重启终端**
   - 右键点击终端
   - 选择"Kill Terminal"（杀掉终端）
   - 重新打开新终端

3. **重新运行**
   - 按 `F5` 或点击"Run"按钮

#### 方法 2：手动清理进程

**在 PowerShell 或命令提示符中运行：**

```powershell
# 停止所有 dart.exe 进程
taskkill /F /IM dart.exe

# 停止所有 flutter 相关进程
taskkill /F /IM flutter.exe
```

**然后重新启动应用**

#### 方法 3：使用任务管理器

1. 按 `Ctrl + Shift + Esc` 打开任务管理器
2. 找到所有 `dart.exe` 进程
3. 右键 → 结束任务
4. 关闭 VSCode/Cursor
5. 重新打开并运行

### ⚠️ 预防措施

#### 1. 正确停止应用

```bash
# 在终端中按 Ctrl+C 停止
# 或在 VSCode/Cursor 中点击停止按钮
```

#### 2. 清理构建缓存

```bash
cd d:\dov\xinghe_new
flutter clean
flutter pub get
```

#### 3. 重启 Dart Analysis Server

在 VSCode/Cursor 中：
1. `Ctrl + Shift + P`
2. 输入 "Dart: Restart Analysis Server"
3. 选择并执行

### 🚀 完整的重启流程

```bash
# 1. 停止所有进程
taskkill /F /IM dart.exe

# 2. 清理项目（可选）
cd d:\dov\xinghe_new
flutter clean
flutter pub get

# 3. 重启 IDE
# 关闭 VSCode/Cursor，然后重新打开

# 4. 运行应用
flutter run -d windows
# 或按 F5
```

### 🔍 检查端口占用

如果问题持续，可能是端口被占用：

```powershell
# 检查常用端口
netstat -ano | findstr "8080"
netstat -ano | findstr "3000"

# 如果发现占用，杀掉对应进程
taskkill /F /PID [进程ID]
```

### 💡 常见问题

**Q: 为什么会有多个 dart.exe？**
- Dart Analysis Server（代码分析）
- Flutter 工具
- 运行中的应用
- 之前未关闭的实例

**Q: 是否每次都需要清理？**
- 正常情况下不需要
- 只在进程残留时需要

**Q: F5 的作用是什么？**
- 在 VSCode/Cursor 中：启动调试（Start Debugging）
- 在应用运行时：热重载（Hot Reload）

---

**创建日期**: 2026-01-27
**状态**: 待执行清理
